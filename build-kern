config PIPPO

define PIPPO in c

define CONFIG_PIPPO as var in makefile

obj-$(CONFIG_PIPPO) where CONFGI_PIPPO can be y, n, m

kallsyms -> flags column T = global, t = a static fn, D = global data, d = local data for .o, R/r sma eof D/d but for read-only

%pK to print kern ptr that can't be viewed only from root


